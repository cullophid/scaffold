// Generated by CoffeeScript 1.10.0
(function() {
  var cleanup, git, logger, npm, updatePackage;

  npm = require('./npm');

  updatePackage = require('./update-package');

  git = require('./git');

  cleanup = require('./cleanup');

  logger = function(key) {
    return function(value) {
      return console.log(key, value);
    };
  };

  module.exports = function(repo) {
    var repoURL;
    repoURL = "https://github.com/" + repo + ".git";
    return npm.init().then(git.clone(repoURL)).then(updatePackage).then(cleanup)["catch"](logger('Error'));
  };

}).call(this);
